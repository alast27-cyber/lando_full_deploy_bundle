replicaCount: 1

image:
  repository: ghcr.io/YOUR-ORG/YOUR-REPO/lando
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  hosts:
    - host: lando.local
      paths:
        - path: /
          pathType: Prefix
  tls: []
